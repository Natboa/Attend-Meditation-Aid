abstract final class AppDurations {
  static const pageTransition = Duration(milliseconds: 300);
  static const fadeIn = Duration(milliseconds: 400);
  static const timerPulseCycle = Duration(seconds: 4);
  static const soundPreview = Duration(seconds: 3);
  static const snackBar = Duration(seconds: 2);
}
